server:
  port: 8081

spring:
  application:
    name: admin-service
  profiles:
    active: dev

  config:
    import: configserver:http://localhost:8888

  cloud:
    config:
      uri: http://localhost:8888/
      username: 
      password: 
      label: main

  datasource:
    driver-class-name: 
    url: 
    username: 
    password: 

